extends: occurrence
message: "Page is over 100,000 characters. Consider splitting it into smaller pages."
level: warning
scope: raw
max: 100000
# Matches any character except newlines. Newlines are excluded from the count,
# so the effective threshold is slightly above 100,000 for files with many blank lines.
token: '.'

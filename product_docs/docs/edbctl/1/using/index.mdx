---
title: "Using edbctl"
navTitle: Using
navigation:
  - edbctl_backup
  - edbctl_catalog
  - edbctl_cluster
  - edbctl_cluster_template
  - edbctl_config
  - edbctl_credential
  - edbctl_disaster_recovery
  - edbctl_hybrid_manager
  - edbctl_image
  - edbctl_image_pull_secret
  - edbctl_location
  - edbctl_operand
  - edbctl_project
---

Use the `edbctl --help or -h` command to return the complete list of commands:

```
edbctl --help
__OUTPUT__
The edbctl command-line interface (CLI) is a tool for managing HCP resources
and performing database cluster operations. You must first configure an access key using the
"credential" command to proceed using this tool. These credentials will allow you to access the supported
HCP resources and manage them using the edbctl CLI. The CLI supports various commands for managing
projects, clusters, backups, credentials, and more.

Usage:
  edbctl [options]
  edbctl [command]

Examples:
edbctl credential import-access-key --name user1 --access-key baak_xyz --address http://localhost:8080
edbctl config set context_project "Adaptable Chameleon"

Management Commands:
  credential        Manage authentication credentials.
  project           Manage projects associated with the database clusters.
  location          Manage locations associated with the projects.
  cluster           Manage database cluster resources.
  cluster-template  Manage database cluster template resources.
  backup            Manage cluster backups.
  catalog           Manage database catalog resources.

Configuration Commands:
  config            Manages configuration settings for edbctl tool
  help              Help about any command

Setup Commands:
  image             Image management commands
  operand           Operand management commands
  image-pull-secret Pull Secret management commands
  hybrid-manager    Hybrid Manager management, monitoring, and diagnostics

Options:
  -h, --help      help for edbctl
  -v, --version   version for edbctl

Use "edbctl [command] --help" for more information about a command.
```

## Common flags

Only `-h/--help` and `-v/--version` are true global flags. The following flags are per-subcommand and fall back to their corresponding config settings when omitted:

- `--credential` (`-c`) — falls back to `context_credential`
- `--project` (`-P`) — falls back to `context_project`
- `--output` (`-o`) — falls back to `output_mode`

### Output formats

Most commands accept `-o/--output` with the following values: `table` (default), `json`, `yaml`, `xml`, and `json-raw`. The `json-raw` format returns the unprocessed API response without field renaming, which is useful for scripting and differs from `json`.

## edbctl commands list

-   [edbctl backup](edbctl_backup.mdx)	— Manage cluster backups.
-   [edbctl catalog](edbctl_catalog.mdx)	— Manage database catalog resources.
-   [edbctl cluster](edbctl_cluster.mdx) — Manage database cluster resources.
-   [edbctl cluster-template](edbctl_cluster_template.mdx)	— Manage database cluster template resources.
-   [edbctl config](edbctl_config.mdx)	— Manage configuration settings for `edbctl` tool.
-   [edbctl credential](edbctl_credential.mdx)	— Manage authentication credentials.
-   [edbctl disaster-recovery](edbctl_disaster_recovery.mdx) — Manage disaster recovery operations for HM-internal database clusters.
-   [edbctl hybrid-manager](edbctl_hybrid_manager.mdx) — Hybrid Manager management, monitoring, and diagnostics.
-   [edbctl image](edbctl_image.mdx) — Manage asset images.
-   [edbctl image-pull-secret](edbctl_image_pull_secret.mdx) — Pull secret management commands.
-   [edbctl location](edbctl_location.mdx) — Manage locations for database cluster provisioning.
-   [edbctl operand](edbctl_operand.mdx) — Manage operand images.
-   [edbctl project](edbctl_project.mdx) — Manage projects associated with the database clusters.

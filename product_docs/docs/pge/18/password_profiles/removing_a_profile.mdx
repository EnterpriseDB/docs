---
title: "Dropping a password profile"
description: "How to use the PG_DROP_PROFILE function to drop a password profile"
---

The `PG_DROP_PROFILE` function drops a password profile.

## Synopsis

```sql
PASSWORD_PROFILE.PG_DROP_PROFILE('profilename',
                                 missing_ok)
```
## Description

`PG_DROP_PROFILE` drops the specified password profile for password management.

To use `PG_DROP_PROFILE`, you must not have any users assigned to the profile.

## Parameters

- `profilename` &mdash; Name of the password profile to drop.

- `missing_ok` &mdash; If set to true, the function does not raise an error if the specified profile does not exist. If set to false, an error is raised when the specified profile does not exist.

## See also

[`CREATE PROFILE`](creating_a_profile), [`MODIFYING A PROFILE`](modifying_a_profile)
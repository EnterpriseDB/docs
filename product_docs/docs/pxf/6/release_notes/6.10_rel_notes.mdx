---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: PXF 6.10.2 release notes
navTitle: Version 6.10.2
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/pxf/6x/release_notes/6.10_rel_notes.md
description: Release notes for PXF for WarehousePG version 6.10.2.
editTarget: originalFilePath

---

## PXF 6.10.2

Released: 26 Feb 2026

### Enhancements

-   Added `TIMESTAMPTZ` support to the JDBC profile.
-   Replaced the `adl` profile with `abfss`, for Azure Data Lake Storage Gen2 support.

### Bug fixes

-   Fixed the `pxf.control` and `pxf_fdw.control` extension files installing to the PXF package directory instead of `$GPHOME/share/postgresql/extension`, where WHPG expects them. The package now installs both files to the correct location directly, so `pxf cluster register` is no longer required after a default installation.

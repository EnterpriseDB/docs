---
title: "Managing a PEM Agent"
---

<div id="toc_pem_agent" class="registered_link"></div>


The PEM agent is responsible for implementing scheduled tasks on the PEM server on behalf of the server. The agent runs as a service (on Windows) or as a daemon (on Linux). The PEM server installer automatically installs and configures an agent that is responsible for monitoring the PEM server; you can use the PEM agent installer to add additional agents.

Contents:

<div class="toctree">

pem\_agent\_properties pem\_agent\_binding pem\_agent\_start\_pem\_agent pem\_agent\_ha

</div>

The PEM agent has a number of configuration parameters that control agent behaviors and enable PEM functionality. For a list of the parameters, see:

<div class="toctree">

pem\_agent\_privileges pem\_agent\_config\_params pem\_agent\_self\_registration pem\_agent\_server\_registration

</div>

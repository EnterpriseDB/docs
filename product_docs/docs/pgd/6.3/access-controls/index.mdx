---
title: Setting up access controls
navTitle: Setting up access controls
description: How to configure authentication, SSL/TLS, and Connection Manager endpoints to secure access to a PGD cluster.
navigation:
  - inter-node-connections
  - client-connections
---

Access control in a PGD cluster operates at two levels.

- **[Inter-node connections](inter-node-connections)** are the connections PGD nodes make to each other for replication. Securing these connections ensures that replication traffic is encrypted and that only authorized nodes can join the cluster.

- **[Client application connections](client-connections)** arrive through Connection Manager. Securing these connections controls how applications authenticate, which ports they connect to, and whether the HTTP management endpoint uses TLS.

---
title: Configuring AIDB
navTitle: Configuring
---

To use AIDB, add it to `shared_preload_libraries` and create the extension in the database. 

1. In the `postgresql.conf` file, add `aidb` to the `shared_preload_libraries` parameter:

   ```ini
   shared_preload_libraries = 'aidb'
   ```

   !!! Note
   If `shared_preload_libraries` has other extensions, then you can append `aidb` to the list using a comma as a separator. The order doesn't matter. 
   !!!

1. Restart Postgres. 

1. Create the AIDB extension in your database: 

   ```sql
   CREATE EXTENSION aidb;
   ```
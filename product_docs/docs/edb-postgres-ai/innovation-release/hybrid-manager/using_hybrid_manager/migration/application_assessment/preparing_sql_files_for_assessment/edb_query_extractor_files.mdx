---
title: Using the EDB Query Extractor
navTitle: "EDB Query Extractor"
description: Capture your application's SQL using Oracle Unified Auditing and the EDB Query Extractor script to produce .out files for upload to Hybrid Manager.
deepToC: true
---

The EDB Query Extractor captures SQL executed by your application using Oracle Unified Auditing, which provides persistent storage of audit records — unlike `V$SQL`, which is in-memory and can be cleared between restarts. The extractor exports the captured SQL to a `.out` file compatible with Hybrid Manager (HM)'s assessment engine.

## Prerequisites

-   Oracle Unified Auditing is enabled on the database.
-   You have `AUDIT SYSTEM` privilege, or a DBA can configure the audit policy on your behalf.

## Setting up the audit policy

Create an audit policy that captures all SQL actions for your application's database user:

```sql
CREATE AUDIT POLICY trace_style_policy
    ACTIONS ALL;

AUDIT POLICY trace_style_policy BY YOUR_APP_USER;
```

Replace `YOUR_APP_USER` with the Oracle schema the application connects as. Enabling the policy for a specific user (rather than for all users) is strongly recommended to limit performance impact and output volume.

## Running the application

With the audit policy active, run your application or test suite. Oracle will record all SQL statements executed by the audited user into `UNIFIED_AUDIT_TRAIL`.

## Extracting the SQL

Run the EDB Query Extractor script (`edb_queries_extractor.sql`) against the database to export the captured SQL from `UNIFIED_AUDIT_TRAIL` to a `.out` file. The script is provided as part of the EDB tooling package.

## Disabling the audit policy

After extraction, disable the audit policy to stop capturing new records:

```sql
NOAUDIT POLICY trace_style_policy BY YOUR_APP_USER;
```

Upload the generated `.out` file to HM for assessment.

## Related topics

-   [Preparing SQL files for assessment](../preparing_sql_files_for_assessment) — Overview of all supported SQL file types.
-   [Generating Oracle trace files](oracle_trace_files) — Alternative capture method using Oracle SQL tracing.
-   [Preparing plain SQL files](plain_sql_files) — Upload existing SQL scripts without Oracle-side configuration.
-   [Creating an application record](../../creating_an_application_record) — Upload your extractor output and start the assessment.

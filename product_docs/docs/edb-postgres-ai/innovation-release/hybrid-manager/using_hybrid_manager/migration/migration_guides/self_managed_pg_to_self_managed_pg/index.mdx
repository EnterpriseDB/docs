---
title: Migrate your self-managed Postgres database to a different self-managed Postgres database
navTitle: Self-managed Postgres to self-managed Postgres
description: Learn how to migrate your self-managed Postgres database to a different self-managed Postgres database.
indexCards: none
navigation:
  - '# Set up auxiliary machine'
  - auxiliary_machine
  - '# Prepare source'
  - preparing_source_pg_db
  - '# Assess database'
  - agent_postgres
  - '# Prepare destination'
  - creating_destination_db
  - preparing_destination_selfpg_db
  - '# Migrate schema'
  - handling_schema
  - migrating_schema
  - '# Migrate data'
  - creating_project_credentials
  - configuring_reader
  - viewing_source
  - configuring_writer
  - viewing_destination_console
  - creating_migration
  - monitoring_migration
  - finalizing_migration
  - applying_constraints

---

These are all the steps you need to perform a migration from a self-managed Postgres database to a different self-managed Postgres database using the data migration service (DMS) of Hybrid Manager (HM).

## Set up your auxiliary machine

Configure the machine that hosts both migration agents. This machine acts as the bridge between your Postgres source and HM.

1.  [Configure your auxiliary machine](auxiliary_machine).

## Prepare your source database

Prepare your source database for data migration. This involves creating a dedicated user for the migration and altering the database configuration to enable replication features.

1.  [Prepare the source Postgres database](preparing_source_pg_db).

## Assess the database

Install and configure the EDB Postgres AI agent to register your source database with HM. This step is required — it enables schema migration within the HM landscape in addition to providing observability and high-level migration metrics.

1.  [Configure the EDB Postgres AI agent to assess your database](agent_postgres.mdx).

## Prepare your destination database

Create a server to host the destination database. Then, alter the destination database's configuration to to enable replication features.

1.  [Create a destination database in your server](creating_destination_db).

1.  [Prepare the destination Postgres database](preparing_destination_selfpg_db).

## Migrate your schema

Handle incompatibilities and transfer your schemas.

1.  [Handling constraints for schema migration](handling_schema). 

1.  [Migrating schemas](migrating_schema). 

## Migrate your data

Execute the data transfer and complete the final cleanup steps.

1.  [Create project credentials for the data migration](creating_project_credentials). 

1.  [Configure a DMS agent in reader mode for a data migration](configuring_reader). 

1.  [Verify your source database appears in the HM console](viewing_source).

1.  [Configure a DMS Agent in writer mode for a data migration to a self-managed Postgres database](configuring_writer).

1.  [Verify your destination appears in the HM console](viewing_destination_console).

1.  [Create and perform the data migration](creating_migration).

1.  [Monitor your data migration](monitoring_migration).

1.  [Finalize the migration](finalizing_migration) (for streaming migrations only).

1.  [Apply excluded constraints](applying_constraints). 

---
title: Migrating Postgres schemas
navTitle: Migrating schemas
description: Choose between Simplified (HM-managed) or Manual schema migration, handle constraints, and prepare your schema before data migration begins.
deepToC: true
redirects:
  - /edb-postgres-ai/current/hybrid-manager/using_hybrid_manager/migration/migration_guides/self_managed_pg_to_hm_managed/handling_schema/
---

Before starting a data migration, choose how to handle schema migration. HM supports two approaches:

- **Simplified (recommended)**: HM manages schema migration and constraint handling automatically as part of the data migration setup.
- **Manual**: Export and import the schema yourself using Migration Toolkit or `pg_dump`, handling constraints in two stages.

<TabContainer syncKey="pg_schema_path">
<Tab title="Simplified (recommended)">

No manual schema preparation is required. HM handles schema migration and constraint ordering automatically when you select the **Schema and data** scope later. Before proceeding, confirm:

- The EDB Postgres AI agent has ingested your source schema and a Migration Portal project exists for the database. Verify at **Migrate > Migration Portal** in the HM console.
- When you configure the DMS reader agent in a later step, you'll set `DBCONFIG_DATABASES_<N>__RESOURCEID` to the same `resource_id` value as in `beacon_agent.yaml`. The **Schema and data** and **Schema only** scopes will fail without it.

Continue to the next step.

</Tab>
<Tab title="Manual">

Before you use EDB DMS for data migration, export and import your schema to the destination database. DMS migrates data only — the destination database must have schemas in place before migration begins.

## Handling constraints

Import constraints in two stages to avoid performance degradation during migration:

- **Include now** (before data migration): primary key, unique, and `NOT NULL` constraints. EDB DMS requires primary key and unique constraints to guarantee exactly-once delivery. `NOT NULL` constraints have negligible performance impact.
- **Exclude now, reapply later** (after data migration): foreign key, check, and exclusion constraints. These cause unnecessary CPU and memory overhead while data is actively being migrated.

!!!note
For tables without primary key or unique constraints, EDB DMS provides at-least-once delivery only. You can perform deduplication during migration verification.
!!!

## Exporting your schema

We recommend using [EDB Migration Toolkit](/migration_toolkit/latest/) to export your schema. MTK's [offline migration](/migration_toolkit/latest/07_invoking_mtk/08_mtk_command_options/#offline-migration-options) capability generates SQL scripts you can apply on the destination without direct connectivity. Other valid export methods include `pg_dump`/`pg_restore`, pgAdmin, or `psql`.

When using Migration Toolkit:

- Use the `-offlineMigration` option to generate SQL scripts.
- Use the `-schemaOnly <schema_scope>` option to export schema only.
- Exclude (comment out) foreign key, check, and exclusion constraints from the generated script before importing.
- If you plan to map objects to different names in the destination, edit them directly in the DDL script before importing. Update all dependent objects — views, constraints, and other references — that reference renamed objects. See [Mapping schema, tables, and columns](/edb-postgres-ai/current/hybrid-manager/using_hybrid_manager/migration/migrate/migrations/#mapping-schema-tables-and-columns-during-a-migration) for details.

## Importing your schema

After preparing the DDL and excluding the deferred constraints, connect to the destination database and apply the SQL file by executing the generated [offline migration script](/migration_toolkit/latest/07_invoking_mtk/08_mtk_command_options/#offline-migration-options).

!!!note
Apply the excluded foreign key, check, and exclusion constraints after migration completes. Keep track of them so you can reapply them when the time comes.
!!!

</Tab>
</TabContainer>

## Next step

[Create project credentials for the data migration](creating_project_credentials)

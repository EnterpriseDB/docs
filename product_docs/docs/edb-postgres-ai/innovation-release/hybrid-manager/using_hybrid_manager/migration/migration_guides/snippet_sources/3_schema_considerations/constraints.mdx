---
title: Preparing and importing the schema
navTitle: Preparing schema
deepToC: true
# SINGLE-SOURCE FILE — edits here also affect these pages:
#   - migration_guides/self_managed_pg_to_hm_managed/handling_schema.mdx
#   - migration_guides/aws_rds_pg_to_hm_managed/handling_schema.mdx
#   - migration_guides/self_managed_pg_to_self_managed_pg/handling_schema.mdx
---

Before starting a data migration, choose how to handle schema migration and constraint ordering. HM supports two approaches:

- **Simplified (recommended)**: Let HM manage schema migration and constraint handling automatically as part of creating the data migration.
- **Manual**: Export and import the schema yourself using Migration Toolkit (MTK) or `pg_dump`, handling constraints manually.

<TabContainer syncKey="pg_schema_path">
<Tab title="Simplified (recommended)">

No schema preparation is required. HM handles schema migration and constraint ordering automatically as part of creating the data migration. Proceed to [Creating project credentials for the data migration](creating_project_credentials).

</Tab>
<Tab title="Manual">

Before you use the Data Migration Service (DMS) to configure a data migration, you must prepare and import your schema to the destination database.

Importing the schema to the destination database before the migration takes place is important as DMS migrates only data. The destination database must have the schemas in place for the migration to populate them with data.

Some constraints must be included before the data migration takes place. Others must be applied after the data migration is completed. This approach ensures you can migrate without performance degradation.

## Schema integrity and performance considerations

The presence of destination database constraints, triggers, and WAL logging can impact the data migration performance. When this approach is possible, we recommend a two-step import of schema constraints.

### Primary key and unique constraints

DMS leverages primary key and unique constraints to provide exactly-once delivery when migrating data to the destination database. Therefore, you must include primary key and unique constraints in the schema import that you perform before the data migration begins. Exclude other types of constraints from the schema import.

For rows in tables that don't have primary key or unique constraints you can achieve only at-least-once delivery. You can perform deduplication during the data migration verification.

!!!note
`NOT NULL` constraints don't represent a significant performance impact for destination servers and can also be included in the schema import.
!!!

### Foreign key, check, and exclusion constraints

DMS can apply change events in parallel against destination database clusters. However, migrating some constraint types can negatively affect migration performance. These constraint types lead to unnecessary CPU and memory utilization in the context of an in-flight data migration from a consistent and referentially integral source database.

We recommend applying foreign key, check, and exclusion constraints on the destination database only after the migration is complete.

</Tab>
</TabContainer>

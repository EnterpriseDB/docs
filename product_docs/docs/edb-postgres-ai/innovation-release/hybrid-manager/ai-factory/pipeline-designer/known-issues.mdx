---
title: Known issues and limitations
navTitle: Known issues
description: Current limitations, known issues, and workarounds for Pipeline Designer in Hybrid Manager.
---

This page documents known issues and limitations in Pipeline Designer as of HM 2026.4. Where applicable, workarounds are provided.

## Pipeline creation and structure

### Steps cannot be modified after creation

Once a pipeline is deployed, its step structure (step types, order, and configuration) is fixed. You cannot add, remove, or reorder steps. To change a pipeline's structure, delete it and create a new one.

### Pipeline name length limit

Pipeline names are limited to 46 characters. This is more restrictive than the 50-character limit shown in some AIDB documentation, because AIDB reserves up to 17 characters for internal object name suffixes (state tables, triggers, error logs).

### Destination table name collision

If a table named `pipeline_{your_pipeline_name}` already exists in the target schema and is not a registered knowledge base vector table, pipeline creation will fail. Either rename the existing table or choose a different pipeline name.

## Multi-step pipelines

### ChunkText overlap setting not available

The `overlap_length` parameter for the ChunkText step (which controls how much text overlaps between consecutive chunks) is supported by AIDB but not yet configurable through Pipeline Designer. If you need chunk overlap, create the pipeline through SQL directly.

## Inference routing on remote clusters

### HM-hosted models may not work on remote clusters

When creating pipelines on self-managed PostgreSQL instances or secondary HM locations, HM-hosted KServe models appear in the model picker but may fail at execution time. The inference endpoint runs on the primary HM Kubernetes cluster, and the network path from remote clusters to the KServe service may not be available.

**Workaround**: Use external inference services (OpenAI, AWS Bedrock) or on-database AIDB models for pipelines running on remote clusters. External services are internet-accessible and do not depend on the HM internal network.

### OCR external service

The OCR external service integration has a known issue and may not function correctly. If OCR is required on a remote cluster, consider using an on-database OCR model or processing OCR workloads on the primary cluster.

## PGD cluster issues

### Pipeline creation regression on PGD with AIDB v6.1

AIDB v6.1 (shipped with HM 2026.3) introduced a regression affecting pipeline creation on PGD clusters. The issue stems from a change in when background processing triggers are created: BDR enforces that trigger function owners must match the relation owner on replicated tables, and the new trigger creation timing caused ownership mismatches.

**Affected versions**: HM 2026.3 with AIDB v6.1. Non-PGD clusters are not affected.

**Status**: Fix in progress (AID-1076).

**Workaround**: If you encounter pipeline creation failures on PGD clusters with error messages referencing trigger ownership or BDR constraints, contact support. As a temporary measure, creating pipelines with Disabled processing mode (which does not create triggers) and then manually running them may work.

### Trigger ownership on replicated tables

On PGD clusters, Live and Background processing modes create trigger functions on the source table. BDR requires these triggers to be owned by a role compatible with the table owner across all replication nodes. Ensure that VPU permissions are consistently configured on all nodes. See [VPU and permissions: Tables on PGD clusters](vpu-and-permissions#tables-on-pgd-clusters).

## Processing modes

### Live mode blocks application writes

Live processing executes within the triggering transaction. An INSERT, UPDATE, or DELETE on the source table does not return until the pipeline finishes processing the affected row. For steps involving inference model calls (SummarizeText, KnowledgeBase embedding), this can add significant latency to the application's write path.

**Recommendation**: Use Background mode for production workloads unless real-time processing is strictly required.

### Background worker overhead

The background processing worker polls all databases on the cluster for pipelines in Background mode. On deployments with many databases or many pipelines, this polling can create measurable overhead.

## Knowledge bases

### No standalone knowledge base creation

Knowledge bases can only be created as a side effect of deploying a pipeline with a KnowledgeBase terminal step. There is no standalone "create knowledge base" operation in Pipeline Designer.

### Knowledge base deletion cascades to pipeline

Deleting a knowledge base (when this feature becomes available in a future release with AIDB 7 multi-pipeline KB support) will cascade to all connected pipelines. All associated pipelines will be deleted along with the knowledge base.

## Model and access control

### No per-user model isolation

Model credentials are shared across all database users on a cluster. There is no per-user or per-pipeline credential isolation. See [VPU and permissions: Shared model credentials](vpu-and-permissions#shared-model-credentials) for details.

### No AIDB version awareness gate

Pipeline Designer does not currently detect the installed AIDB version on a target database. If a future AIDB major version changes its SQL API, Pipeline Designer may call incompatible functions without warning. This defensive gate is planned but not yet implemented.

## Feature availability matrix

The following table summarizes when key Pipeline Designer features became available:

| Feature | First available | Notes |
| ------- | --------------- | ----- |
| Single-step pipelines | HM 2025.11 | GA with initial Pipeline Designer release |
| Pipeline view mode | HM 2025.11 | Read-only pipeline detail page |
| PGD cluster support | HM 2025.11 | With known PGD-specific considerations |
| External inference services (backend) | HM 2026.1 | Registration and proxy of external model providers |
| External inference services (UI) | HM 2026.1 | UI for registering external services; refined in 2026.2 |
| Multi-step pipelines | HM 2026.3 | Up to 10 steps per pipeline with ordering validation |
| Pipelines on secondary HM locations | HM 2026.4 | Multi-DC pipeline support |
| Pipelines on self-managed PostgreSQL | HM 2026.4 | Requires the EDB Postgres AI agent (`beacon-agent`) and AIDB on the target instance |
| Per-AIDB-instance model discovery | HM 2026.4 | Models queried from the target database, not centrally |

---
title: "Hybrid Manager 2026.5.0 release notes"
navTitle: "Version 2026.5.0"
description: "Release notes for version 2026.5.0 of Hybrid Manager."
deepToC: true
---

This Innovation Release includes features and enhancements.

**Release Date:** May 13, 2026

!!!info Release cadence update

HM now follows a dual release strategy: **LTS** and **Innovation Releases**. For details on this new model, see [Hybrid Manager (HM) dual release strategy](/edb-postgres-ai/current/hybrid-manager/release_notes/#hybrid-manager-dual-release-strategy).
!!!

## Release highlights

-   **Postgres-to-Postgres schema migration:** The Data Migration Service (DMS) now supports complete schema migrations between Postgres databases, including proper handling of Postgres-specific types such as custom domain types.

-   **Enhanced agentic migration:** The Migration Agent Langflow template now uses an improved architecture leveraging HM's unified schema migration feature, with added support for Llama 3 Nemotron 49B and Nano 4B models.

-   **HM chatbot enhancements:** Added Anthropic Claude as a chatbot provider and bundled HM and PostgreSQL documentation for citation-backed answers.

-   **Role mapping improvements:** You can now assign multiple roles in a single role mapping rule, streamlining identity provider group-to-role assignments.

## Release details

### AI Factory

| Type | Description | Jira ref. |
| ---- | ----------- | --------- |
| Enhancement | Added support for [AIDB 7](/aidb/latest/) in the AI Pipeline Designer, including vector schema and vector table fields in the knowledge base list API to display valid destinations when creating pipelines with existing knowledge base tables. Improved knowledge base validation during pipeline creation by checking existence directly at the database level, which provides more accurate validation and supports knowledge base tables in any schema. Fixed issues whereby AI pipelines with existing knowledge base tables remained stuck in "Configuring" status indefinitely, and the clusterId filter in the list knowledge bases API wasn't working correctly for EDB Postgres Distributed (PGD) clusters. | UPM-84336 |
| Enhancement | Added support for registering external inference services with insecure connections. You can now enable an **Allow insecure connection** option when registering external model services that don't have signed certificates, such as those in test environments. The system requires explicit user acknowledgment of the security risk before allowing the registration. | UPM-94849 |
| Enhancement | Enhanced error handling when publishing AI flows with invalid parameters. The UI now displays detailed error messages from the server instead of a generic "Failed to publish flow" message, helping you identify and fix parameter naming issues. | UPM-97335 |
| Change | Removed the AI Pipelines Default Settings page from the Sovereign AI section. The default settings feature was incompatible with the new instance-specific models list approach. | UPM-87969 |
| Bug fix | Resolved a startup failure in the EDBAirman component where uvx dependency resolution errors prevented the Model Context Protocol (MCP) server from initializing. The runtime now includes all required dependencies. | AID-2054 |

### HM console

| Type | Description | Jira ref. |
| ---- | ----------- | --------- |
| Enhancement | Enhanced the role mapping rules interface to support selecting multiple roles for a single rule. You can now assign multiple roles (such as both Project Viewer and Catalog Data Writer) to the same group and identity provider combination in a single rule, rather than creating separate rules for each role. | UPM-86415 |
| Enhancement | Added Anthropic Claude as a supported provider for the HM chatbot. The chatbot now renders Mermaid diagrams inline in responses. Bundled HM and Postgres (16/17/18) documentation with the chatbot so it can answer product questions with citations. | UPM-94976 |

### Observability and monitoring

| Type | Description | Jira ref. |
| ---- | ----------- | --------- |
| Bug fix | Thanos Ruler used the default Kubernetes service account. It now uses a dedicated service account. | UPM-97404 |

### Migrations

| Type | Description | Jira ref. |
| ---- | ----------- | --------- |
| Enhancement | Added concurrent schema finalization for `schema_and_data` migrations, allowing you to start schema operations while data migration continues running. You can now select **Finalize Schema Concurrently** on the migration detail page after the snapshot stage completes, which applies indexes, foreign keys, and other schema objects in the background using non-blocking data definition language (DDL) variants. The migration detail page displays concurrent schema finalization progress alongside existing snapshot and streaming indicators. When you later complete the migration, only lightweight validation steps remain, significantly reducing cutover time compared to waiting for all schema operations during the completion phase. | ET-5022 |
| Enhancement | You can now migrate schemas from Postgres to Postgres using the DMS. The DMS now handles Postgres-specific database objects—including custom domain types and other Postgres-native type definitions—during schema migration, ensuring the DMS applies schema objects to the target before other schema objects that depend on them. This enables complete, end-to-end schema migration between Postgres databases. | MIG-8858 |
| Enhancement | Enhanced the Simple Migration Agent Langflow template with an improved architecture that leverages HM 1.4's unified schema migration feature, replacing the legacy Migration Portal approach. The updated workflow now includes streamlined agent components, automatic password generation and database setup, and removal of redundant components. Added support for Llama 3 Nemotron 49B and Llama 3 Nemotron Nano 4B models, providing additional AI model options for migration workflows. Enhanced database password security by moving cluster creation operations outside of large language model (LLM) processing and leveraging global secrets management to prevent credential exposure. Fixed an issue whereby the list of tools in a MCP Tools component wouldn't update after selecting an MCP server until you refreshed the page. | MIG-8057 |
| Enhancement | Optimized the migration creation form to skip unnecessary table loading and validation when you set the migration scope to schema-only. The form now only fetches table data for data-inclusive migration scopes, improving performance and user experience. | ET-5050 |
| Bug fix | Added support for migrating EDB Postgres Advanced Server (EPAS) PACKAGE and PACKAGE BODY schema objects from EPAS source databases to EPAS destination databases. The DMS now properly handles package and package body statements during schema migration, including correct dependency ordering to ensure the DMS applies package bodies after the tables they reference. | MIG-8903 |
| Bug fix | Added support for migrating EPAS non-public `SYNONYM` schema objects from EPAS source projects. The DMS now properly handles non-public synonym statements during schema migration. The DMS extracts and uploads `PUBLIC SYNONYM` objects into the Migration Portal project but doesn't yet support migrating them to the destination database. | MIG-8905 |
| Bug fix | Fixed an issue whereby the schema migration capability couldn't migrate database objects for schemas that didn't contain any tables. The migration process now handles all schemas regardless of whether they include tables. | ET-5159 |
| Bug fix | Fixed an issue whereby schema-only migrations dropped `PRIMARY KEY` constraints and their dependent foreign key and named CHECK constraints when you disabled data migration for specific schemas but enabled it for others. The transporter now correctly applies `PRIMARY KEY` constraints during the finalize schema phase for tables excluded from data migration. | ET-5483 |
| Bug fix | Fixed an issue whereby the DMS validation incorrectly required all schemas in the source database to have a healthy Migration Portal project, even when the migration didn't include those schemas. The validation now only checks schemas that the migration's table list references. | MIG-8918 |
| Bug fix | Fixed an issue whereby the transformed SQL file had missing line endings when the source SQL file contained Windows-style CR LF line endings. | MIG-8301 |

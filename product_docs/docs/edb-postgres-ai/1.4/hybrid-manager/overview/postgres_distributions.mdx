---
title: Supported Postgres distributions
navTitle: Postgres distributions
description: The different flavors of Postgres distributions supported in Hybrid Manager.
---

Selecting the right Postgres distribution when creating a new database cluster in Hybrid Manager (HM) is crucial for optimizing your database environment. EDB offers a range of options, each tailored to different needs, from open-source flexibility to enterprise-grade features and Oracle compatibility.

!!!note
In the EDB Postgres AI naming hierarchy, "Enterprise Postgres" refers to EDB Postgres Extended Server, and "Enterprise Postgres Oracle Compatible" refers to EDB Postgres Advanced Server. You may see both naming conventions in the console and documentation.
!!!

## EDB Postgres Advanced Server

EDB Postgres Advanced Server is EDB's most comprehensive PostgreSQL distribution. It supports features such as [advanced security](/epas/latest/epas_security_guide), [advanced SQL](/epas/latest/application_programming/15_enhanced_sql_and_other_misc_features/), high-availability and multi-master replication in combination with [EDB Postgres Distributed](/pgd/latest) and, optionally, [Oracle compatibility](/epas/latest/working_with_oracle_data). It's ideal for demanding enterprise workloads and seamless migration from cost-intensive Oracle databases.

[SME CONFIRM, claim 19: eng doc states native Oracle compatibility "reduces application rewrites by up to 95%". The eng doc's own verification_notes flag distribution claims as conceptual and not spot-checked against a live cluster, despite the frontmatter's live_validated status. Please confirm this figure before merge.]: #
Native Oracle compatibility can reduce application rewrites by up to 95%.

This option is suited for:

- Enterprises migrating from Oracle databases seeking high compatibility to reduce migration costs and complexity.
- Organizations with mission-critical applications requiring advanced security, performance, and management tools.
- Businesses that need comprehensive enterprise-grade features, including enhanced auditing, SQL injection protection, and robust high availability.
- Teams leveraging the AI Copilot for automated Oracle schema conversion during migration.

Key features:

- **Oracle compatibility mode** — Run Oracle PL/SQL, use Oracle-compatible data types (`VARCHAR2`, `NUMBER`, `DATE`), and use Oracle SQL syntax including hierarchical queries (`CONNECT BY`), bulk operations (`FORALL`, `BULK COLLECT`), and packages. AI-driven recommendations help assess and resolve schema incompatibilities during migration.
- **Advanced security** — Enhanced auditing (EDB Audit), SQL injection protection, data redaction, and virtual private database (VPD) for row-level security policies.
- **Advanced SQL** — Enhanced SQL features beyond community PostgreSQL, including hierarchical queries, bulk operations, autonomous transactions, and a PL/SQL-compatible procedural language.
- **EDB Postgres Distributed (PGD)** — Multi-master replication for active-active high-availability deployments.
- **Transparent Data Encryption (TDE)** — Data-at-rest encryption.
- **Profile management** — Database-level user profile management with password complexity rules, failed login lockout, and session limits.
- **Resource management** — EDB Resource Manager for controlling CPU, memory, and I/O allocation per user or workload group.

## EDB Postgres Extended Server

EDB Postgres Extended Server provides specific enhancements designed for enterprise requirements, particularly in data security, for example, through [transparent data encryption](/tde/latest). It also supports advanced replication with EDB's powerful high-availability and multi-master replication solution, [EDB Postgres Distributed](/pgd/latest). 

This option is suited for:

- Enterprises requiring enhanced data-security features.
- Organizations looking to leverage EDB's high-availability and multi-master replication solutions.
- Users who want a PostgreSQL experience with additional enterprise capabilities while maintaining strong compatibility with community PostgreSQL.

Key features:

- **Transparent Data Encryption (TDE)** — Encrypt data at rest at the storage level without application changes. Supports AES-128 and AES-256 encryption with external key management integration.
- **EDB Postgres Distributed (PGD)** — Multi-master replication for active-active high-availability deployments across multiple Kubernetes clusters or data centers. Enables Distributed HA (DHA) cluster types in Hybrid Manager.
- **WAL encryption** — Write-ahead log encryption to protect data in transit between primary and replica nodes.
- **Enhanced authentication** — SCRAM-SHA-256 authentication with additional enterprise authentication method support.

## PostgreSQL

Open-source PostgreSQL is a powerful, reliable, and feature-rich relational database system. It's known for its strong adherence to SQL standards, robust architecture, data integrity, and a global community. When deployed through Hybrid Manager, community PostgreSQL benefits from the same automated provisioning, monitoring, backup, and lifecycle management as the EDB distributions.

This option is suited for:

- Organizations prioritizing cost effectiveness and open-source freedom.
- Projects that don't require advanced security features, multi-master replication solutions, or Oracle compatibility.
- Teams with in-house PostgreSQL expertise and the resources to manage their database environment.

[SME CONFIRM, claim 12: eng doc specifically calls out "transparent data encryption" as the security feature PostgreSQL lacks, where this page uses the broader term "advanced security features." Please confirm whether these mean the same thing or whether this line needs to be more specific — flagged as an open question, not changed pending your input.]: #

Key features:

- **Standards compliance** — Full SQL:2016 core conformance and extensive SQL:2023 feature support.
- **Extensibility** — Rich extension ecosystem including pgvector, PostGIS, pg_stat_statements, and hundreds of community extensions.
- **Logical replication** — Built-in logical replication for read replicas and data distribution.
- **Partitioning** — Declarative table partitioning (range, list, hash) for managing large datasets.
- **JSON support** — Native JSONB type with indexing and query operators for semi-structured data.

## Postgres version support

The following table shows the Postgres major versions available for each distribution in Hybrid Manager. Specific minor versions depend on the HM release — check the release notes for version-specific details.

| Distribution | Postgres 14 | Postgres 15 | Postgres 16 | Postgres 17 |
| --- | --- | --- | --- | --- |
| PostgreSQL | Supported | Supported | Supported | Supported |
| EDB Postgres Extended Server | Supported | Supported | Supported | Supported |
| EDB Postgres Advanced Server | Supported | Supported | Supported | Supported |

!!!note
Postgres 14 is approaching end of community support. Plan migrations to Postgres 16 or 17 for new deployments. EDB provides extended support for older major versions on EDB Postgres Extended Server and EDB Postgres Advanced Server — see your support agreement for details.
!!!

## Extensions for AI and analytics workloads

Hybrid Manager makes AI-relevant extensions available across distributions to support the AI Factory workload:

- **pgvector** — Vector similarity search for embeddings. Supports IVFFlat, HNSW, and flat index types. Essential for retrieval-augmented generation (RAG) pipelines. Available on all distributions.
- **pgvector scaling** — Distributed vector search for large-scale embedding workloads that exceed single-node capacity. Available on all distributions.
- **pg_embedding** — Alternative vector search implementation with additional distance metrics. Available on all distributions.

Lakehouse and analytics capabilities require specific extensions that are pre-configured in Lakehouse cluster types:

- **PGFS (Postgres File System)** — Exposes object storage (S3, GCS, Azure Blob) as a Postgres-native file system for direct SQL access to external files.
- **Apache Iceberg support** — Read and write Apache Iceberg tables with full support for schema evolution, time travel, and partition pruning.
- **Analytics Engine extensions** — Columnar scan optimization, vectorized execution, and analytical query processing extensions.
- **Tiered table extensions** — Automated data tiering between hot Postgres storage and cold object storage with transparent query access.

[SME CONFIRM, claims 37 through 47: eng doc includes a full per-distribution feature comparison matrix (open-source core, TDE, PGD, Oracle compatibility, advanced security, advanced SQL, profile/resource management, pgvector, Lakehouse extensions, extension ecosystem, HA cluster types). Not added here because the "Distribution comparison" section below already links out to dedicated comparison pages (feature_support_per_db, advanced_replication, advanced_security, advanced_sql, advanced_performance, oracle_compatibility, tools_extensions, deployment_options), which were out of scope for this pass. Please confirm whether that matrix already lives on one of those pages, or whether it should be added here or there.]: #

## Distribution comparison

For a comparison of the available distributions by functionality and feature, see:

- [Feature support per distribution](/edb-postgres-ai/databases/feature_support_per_db)
- [Advanced replication](/edb-postgres-ai/databases/advanced_replication)
- [Advanced security](/edb-postgres-ai/databases/advanced_security)
- [Advanced SQL](/edb-postgres-ai/databases/advanced_sql)
- [Advanced performance](/edb-postgres-ai/databases/advanced_performance)
- [Oracle support](/edb-postgres-ai/databases/oracle_compatibility)
- [Tools and extensions](/edb-postgres-ai/databases/tools_extensions)
- [Deployment options](/edb-postgres-ai/databases/deployment_options)

## Documentation by distribution

To get the most out of any of the Postgres distributions, refer to its documentation:

- [PostgreSQL](https://www.postgresql.org/docs/current/)
- [EDB Postgres Extended Server](/pge/latest/)
- [EDB Postgres Advanced Server](/epas/latest/)

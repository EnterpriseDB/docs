---
title: AI agent governance and audit
navTitle: Governance
description: Audit and govern how AI agents interact with your Hybrid Manager Postgres data — reconstruct agent sessions, inspect every SQL statement, and review the full audit trail.
indexCards: simple
navigation:
  - concepts
  - connecting_data_sources
  - sessions_list
  - session_detail
  - architecture
  - security_and_data
  - administration
---

The AI governance audit log viewer provides observability and oversight for AI agents that operate on Postgres databases managed by Hybrid Manager (HM). 
It lets database administrators, security teams, and AI operations engineers monitor, audit, and reason about how AI agents interact with enterprise data.

The viewer connects to an HM instance (or to a standalone Grafana Loki endpoint) to retrieve the Postgres query logs that AI agents generate through the Airman Model Context Protocol (MCP) server. It presents those logs as structured *sessions* — logical groupings of SQL statements executed within a single AI agent interaction — and provides tools for filtering, inspecting, and auditing agent behavior.

## What you can do

- **Reconstruct agent sessions** — See every SQL statement an AI agent executed, with full metadata such as duration, target database, Postgres role, and error severity.
- **Audit across multiple data sources** — Connect to several HM or Loki sources at once and audit clusters across all of them from one place.
- **Read a flat audit trail** — Scan every statement in a session as a single filterable table, optimized for review.
- **Rely on cached, incremental sync** — The viewer's backend incrementally syncs and caches session data, so audit views load quickly without re-querying the full log history on every visit.

## How the viewer is organized

You start from a list of clusters, open a cluster to see the AI agent sessions detected for it, and open a session to inspect each SQL statement:

```
Clusters  →  Sessions (per cluster)  →  Session detail
                                          ├─ Reconstruction (step-by-step timeline)
                                          └─ Audit log (flat table)
```

## Where to start

- [Concepts](./concepts) — the key terms used throughout the viewer: sessions, steps, purpose, instances, and how Airman MCP tags activity.
- [Connecting data sources](./connecting_data_sources) — register an HM or Loki instance so its agent sessions appear in the viewer.
- [Sessions list](./sessions_list) — find and filter the AI agent sessions detected for a cluster.
- [Session detail](./session_detail) — inspect a single session step by step and read its audit log.
- [Architecture and data flow](./architecture) — how agent activity travels from Postgres logs into the viewer.
- [Security and data handling](./security_and_data) — authentication, credential storage, and how your data is treated.
- [Deployment and configuration](./administration) — for administrators deploying and configuring the viewer.

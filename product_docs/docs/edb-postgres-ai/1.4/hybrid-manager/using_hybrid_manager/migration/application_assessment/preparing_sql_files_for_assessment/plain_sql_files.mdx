---
title: Preparing plain SQL files
navTitle: "Plain SQL files"
description: Format requirements for uploading plain .sql files for application SQL assessment in Hybrid Manager.
deepToC: true
---

If you already have SQL scripts — for example, scripts used for database initialization, maintenance jobs, or SQL embedded in source code files — you can upload them directly as plain `.sql` files without any Oracle-side configuration.

## Format requirements

Plain SQL files must follow these formatting rules for Hybrid Manager (HM) to parse them correctly:

-   Every SQL statement must end with a semicolon (`;`).
-   Every PL/SQL block (anonymous blocks, stored procedures, and triggers) must end with a forward slash (`/`) on its own line, preceded by a blank line.
-   Files must use UTF-8 encoding.

**Example:**

```sql
SELECT employee_id, first_name FROM employees WHERE department_id = 10;

BEGIN
    UPDATE employees SET salary = salary * 1.1 WHERE department_id = 10;
    COMMIT;
END;

/
```

## Related topics

-   [Preparing SQL files for assessment](../preparing_sql_files_for_assessment) — Overview of all supported SQL file types.
-   [Generating Oracle trace files](oracle_trace_files) — Capture runtime SQL using Oracle SQL tracing.
-   [Using EDB Query Extractor](edb_query_extractor_files) — Capture runtime SQL using Oracle Unified Auditing.
-   [Creating an application record](../creating_an_application_record) — Upload your SQL files and start the assessment.

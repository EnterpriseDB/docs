---
title: How-to  Query the cold data as a single table with third-party tools
navTitle: Cold data (third-party)
description: Use Spark to list catalog views and query the offloaded-only view when supported.
---

See `verify-catalog-with-pyiceberg-pyspark.mdx` Spark example. Engines may list views and/or query them. If views aren’t queryable, read underlying Iceberg tables.


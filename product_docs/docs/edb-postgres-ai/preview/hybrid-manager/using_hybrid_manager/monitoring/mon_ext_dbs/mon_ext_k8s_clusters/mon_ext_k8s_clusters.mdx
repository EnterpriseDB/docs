---
title: PGAI agent Installation
navTitle: Installing the PGAI agent
description: Installing the PGAI agent 
deepToC: true
---

## Installation Methods

### Environment-Specific Configuration

This chart is designed to work across different Kubernetes platforms (including OpenShift) with a unified configuration:

| Environment         | Configuration                                   | OpenShift Flag                           |
|---------------------|-------------------------------------------------|------------------------------------------|
| KIND, GKE, EKS, AKS | Uses image defaults (numeric or named)          | openshift.enabled: false (default)       |
| OpenShift (RHOS)    | Compatible via runAsUser: null (SCC assigns ID) | openshift.enabled: false/true (optional) |

Key points:

- **Universal Compatibility:** The chart defaults to runAsUser: null, allowing OpenShift SCCs to assign valid UIDs automatically, while standard Kubernetes falls back to the image's default user.
- **Security Context:** runAsNonRoot strategy varies by component to ensure compatibility with standard K8s validation logic vs OpenShift named-user support.
- **OpenTelemetry collector:** Uses the image default user (nonroot) without explicit numeric IDs to satisfy both strict K8s validation and OpenShift SCCs.

For OpenShift deployments, the default configuration is sufficient.

```
helm upgrade --install beacon-agent-k8s \
  . \
  --create-namespace \
  --namespace upm-beacon \
  --values custom-values.yaml
```

!!!note
Enabling the `openshift.enabled=true` flag is only required when using deprecated templates.
!!!

### Installing Helm chart from repository with customized values file

1. Download the PGAI agent chart from the EDB repository.

   [Ask for additional instruction on how to do it]: #

1. Create a custom-values.yaml file that includes your HM access credentials:

   ```
1. Enter your access key and the machine you created in the HM console:

   ```
   agentConfig:
     accessKey: "baak_xxxxxxxxxxxxxxxxxxxxx"  
     projectID: "your_project_id"              
     beaconServer:
       host: "your_hcp_instance.example.com"
       plaintext: false
     metrics:
       push:
         endpoint: "your_hcp_instance.example.com"
     webhook:
       enabled: true
   ```
  
1. Configure the helm chart for EDB Postgres for Kubernetes:
  
   ```
   podSelector: "k8s.enterprisedb.io/podRole=instance"
   ```
   
   Or open-source CloudNativePG:
   
   ```
   podSelector: "cnpg.io/podRole=instance"
   ```

1. Configure Docker registry credentials for EDB images:

   ```
   image:
     repository: <repo url>
     tag: ""
     imageCredentials:
       registry: "repository-url"
       username: "your_username"
       password: "your_password"
   ```

1. Specify the OpenTelemetry Collector image repository and tag in the following configuration:

   ```
   otelImage:
     repository: <repo url>
     tag: ""
   ```

1. Download the chart to Cluster B (if not already present), then extract the archive and enter the folder to access the configuration files:
   
    ```
    tar -xzf beacon-agent-k8s-<version>.tg
    cd beacon-agent-k8s
    ```
   
1. Install and enable Openshift.

!!!note
   Enabling additional monitoring components is optional.
   Set the values to true to enable them:
    
    ```
    additionalCharts:
      cert-manager:
        install: false
    ```
    Enable for metrics collection:
    
    ```
      opentelemetry-collector:
        install: false  
    ```

    Enable for Kubernetes state metrics:
    
    ```
      kube-state-metrics:
        install: false
    ```
    Enable for node metrics (requires privileged access):
    ``` 
      prometheus-node-exporter:
        install: false  
    ```

   If `opentelemetry-collector.install` is set to `true`, you must define the repository URL and tag for the collector image:
      
    ```
    opentelemetry-collector:
      image:
        repository: <repo url>
        tag: ""
    ```

### Installing the Helm chart with customized values file


1. Install the Helm chart for standard Kubernetes (KIND, GKE, EKS, AKS):
   
      ```
      helm upgrade --install beacon-agent-k8s \
        . \
        --create-namespace \
        --namespace upm-beacon \
        --values custom-values.yaml
      ```

1. Deploy the PGAI agent o cluster B using the OpenShift (RHOS) configuration:
   
      ```
      helm upgrade --install beacon-agent-k8s \
        . \
        --create-namespace \
        --namespace upm-beacon \
        --set openshift.enabled=true \
        --values custom-values.yaml
      
      # Grant SCC if using opentelemetry-collector:
      oc adm policy add-scc-to-user privileged \
        system:serviceaccount:upm-beacon:beacon-agent-k8s-opentelemetry-collector
      kubectl -n upm-beacon rollout restart daemonset beacon-agent-k8s-opentelemetry-collector-agent
      ```

### Installing the Helm chart with the CLI

    Deploy the Beacon Agent to Cluster B by passing the monitoring components, credentials, and agent configuration as command-line arguments:
      
      ```
      helm upgrade --install beacon-agent-k8s \
        --create-namespace \
        --set additionalCharts.cert-manager.install=true \
        --set additionalCharts.opentelemetry-collector.install=true \
        --set additionalCharts.kube-state-metrics.install=true \
        --set image.imageCredentials.registry="${registry}" \
        --set image.imageCredentials.username="${username}" \
        --set image.imageCredentials.password="${password}" \
        --set image.repository=<repo url> \
        --set image.tag=<image tag> \
        --set otelImage.repository=<repo url> \
        --set otelImage.tag=<image tag> \
        --set opentelemetry-collector.image.repository="${repo url}" \
        --set opentelemetry-collector.image.tag=${image tag} \
        --set agentConfig.beaconServer.host="${server_url}" \
        --set agentConfig.metrics.push.endpoint="${server_url}" \
        --set agentConfig.accessKey="${access_key_agent}" \
        --set agentConfig.projectID="${project_agent}" \
        --set rootCA="${cacert}" \
        --set agentConfig.webhook.podSelector="k8s.enterprisedb.io/podRole=instance" \ # for cnp/pg4k cluster
        # --set agentConfig.webhook.podSelector="cnpg.io/podRole=instance" \ # for cnpg cluster
        .
      ```

## Creating and connecting external CNP clusters

You can create an external CNP cluster and connect it to an instance in HM.

### Requirements

- HM cluster: Make sure your HM instance is active and the PGAI agent server is running and reachable.
- External CNP cluster: Confirm that the target Kubernetes cluster has the CNP or CNPG operator pre-installed.

1. Get your external Kubernetes cluster ready by deploying the CNP or CNPG operator.

   Create a namespace in the CNP cluster if it doesn't exist yet:
   
   ```
   export cluster_ns="test-cnp"
   kubectl create ns "${cluster_ns}"
   ```
   See [Install the appropriate PostgreSQL operator for operator installation](/edb-postgres-ai/preview/hybrid-manager/using_hybrid_manager/monitoring/mon_ext_dbs/mon_ext_k8s_clusters/Installation%20requirements/#installing-the-appropriate-postgresql-operator).

1. Get your access key for the PGAI agent
   In the HM console, create a machine user and assign the Estate Ingest Role to the machine user you created.
   
   !!!note
   Ensure that the access key's respective machine user is assigned the Estate Ingest Role. This is required for both the Beacon Agent and the external CNP cluster.
   !!!

   Set the environment variable in the CNP cluster:

   ```
   export access_key_agent="<ACCESS_KEY_AGENT>"
   export project_agent="<PROJECT_ID_AGENT>"
   ```

1. Get your access key for external PGDGroups
   Create another machine user for the external CNP cluster and assign the Estate Ingest Role to the machine user you created.

   !!!note 
   Ensure that the access key's respective machine user is assigned the Estate Ingest Role. This is required for both the Beacon Agent and the external CNP cluster.
   !!!

   Set the environment variables for the PGDGroups :

   ```
   export access_key_pgdgroup="<ACCESS_KEY_PGDGROUP>"
   export project_pgdgroup="<PROJECT_ID_PGDGROUP>"
   ```

1. Create a secret for external PGDGroups

  With the access key you created in HM, create a secret for the PGDGroup in the CNP cluster:

   ```
   kubectl create secret generic beacon-access-key \
       --from-literal=access-key="${access_key_cluster}" -n "${pgdgroup_ns}"
   ```

1. Extract and configure the TLS certificates from the HM cluster

   ```
   kubectl get secret -n edbpgai-bootstrap beaconator-ca-bundle -o jsonpath='{.data.public\.crt}' | base64 -d > temp.crt
   ```

1. Transfer the temp.crt certificate the CNP cluster

1. On the CNP cluster for the agent (used in helm install) run: 
 
 [Need more clarification on this]: #

   ```
   export cacert=$(cat temp.crt)
   
1. On Cluster CNP: For the cluster
   [Ask for clarification on this]: #

   ```
   kubectl create secret generic beacon-server-ca \
       --from-literal=ca.crt="$(cat temp.crt)" -n "${pgdgroup_ns}"
   ```

1. Get the server host and port from the HM cluster

   ```
   export server_host=$(kubectl -n upm-beacon get gateways.networking.istio.io beacon-server -oyaml | yq '.spec.servers[0].hosts[0]')
   export server_port=$(kubectl -n upm-beacon get gateways.networking.istio.io beacon-server -oyaml | yq '.spec.servers[0].port.number')
   export server_url="${server_host}:${server_port}"
   ```

1. Set the server URL on the CNP cluster:

   ```
   export server_url="<server_url>"  #Output of previous step
   ```

1. Get the image registry credentials on the CNP cluster
   
   ```
    export registry=docker.enterprisedb.com
    export username="${USERNAME}"
    export password="${PASSWORD}"
   ```

   !!!note
   For customer deployment, you need to use the CloudSmith registry.
   !!!

1. Install the PAGI Agent by downloading the chart:

   ```
   tar -xzf beacon-agent-k8s-<version>.tg
   cd beacon-agent-k8s
   ```

   !!! note 
   For CNP or PG4K clusters, use: `--set agentConfig.webhook.podSelector="k8s.enterprisedb.io/podRole=instance"`
   For CNPG clusters, use: `--set agentConfig.webhook.podSelector="cnpg.io/podRole=instance"`
   !!!

1. Create a PGDGroup with the beacon selector enabled:

   ```
   export cluster_name=cluster-example-test
   ```

1. Configure the self-signed certificate authority (CA) and required issuers for the Postgres Distributed (PGD) cluster:
  
   ```
   kubectl apply -f -n ${pgdgroup_ns} - <<EOF
   apiVersion: cert-manager.io/v1
   kind: Issuer
   metadata:
     name: selfsigned-issuer
   spec:
     selfSigned: {}
   ---
   apiVersion: cert-manager.io/v1
   kind: Certificate
   metadata:
     name: server-ca
   spec:
     isCA: true
     commonName: my-selfsigned-server-ca
     secretName: server-ca-key-pair
     privateKey:
       algorithm: ECDSA
       size: 256
     issuerRef:
       name: selfsigned-issuer
       kind: Issuer
       group: cert-manager.io
   ---
   apiVersion: cert-manager.io/v1
   kind: Issuer
   metadata:
     name: server-ca-issuer
   spec:
     ca:
       secretName: server-ca-key-pair
   ---
   apiVersion: cert-manager.io/v1
   kind: Certificate
   metadata:
     name: client-ca
   spec:
     isCA: true
     commonName: my-selfsigned-client-ca
     secretName: client-ca-key-pair
     privateKey:
       algorithm: ECDSA
       size: 256
     issuerRef:
       name: selfsigned-issuer
       kind: Issuer
       group: cert-manager.io
   ---
   apiVersion: cert-manager.io/v1
   kind: Issuer
   metadata:
     name: client-ca-issuer
   spec:
     ca:
       secretName: client-ca-key-pair
   EOF
   ```

1. Define your database topology,including instances, proxies, and TLS connectivity settings—within this primary manifest:
   ```
   kubectl apply -f - <<EOF
   apiVersion: pgd.k8s.enterprisedb.io/v1beta1
   kind: PGDGroup
   metadata:
     namespace: ${pgdgroup_ns}
     name: ${pgdgroup_name}
     labels:
       k8s.enterprisedb.io/beacon: enabled
   spec:
     inheritedMetadata:
       labels:
         k8s.enterprisedb.io/beacon: enabled
     instances: 2
     proxyInstances: 2
     witnessInstances: 1
     imageName: docker.enterprisedb.com/k8s/edb-postgres-extended-pgd:17.6-pgd590-ubi9
     imagePullSecrets:
     - name: registry-pullsecret
     pgd:
       parentGroup:
         name: world
         create: true
       globalRouting: true
     cnp:
       initDBOptions:
         encoding: UTF8
         walSegmentSize: 64
       storage:
         size: 2Gi
       postgresql:
         parameters:
           "bdr.global_lock_timeout": "20min"
           "min_wal_size": "200"
           "pgaudit.log": "all, -misc"
           "pgaudit.log_catalog": "off"
           "pgaudit.log_parameter": "on"
           "pgaudit.log_relation": "on"
           # enable more logs for postgres
           "bdr.debug_level": log
           "log_min_messages": debug5
           "log_statement": all
           "log_connections": "on"
           "log_disconnections": "on"
           "log_checkpoints": "on"
       env:
         - name: AWS_REQUEST_CHECKSUM_CALCULATION
           value: when_required
         - name: AWS_RESPONSE_CHECKSUM_VALIDATION
           value: when_required
     witness:
       initDBOptions:
         encoding: UTF8
         walSegmentSize: 16
       storage:
         size: 1Gi
       postgresql:
         parameters:
           "bdr.global_lock_timeout": "20min"
           "min_wal_size": "100"
     pgdProxy:
       imageName: docker.enterprisedb.com/k8s/edb-pgd-proxy:5.9.0-ubi9
     connectivity:
       dns:
         additional:
           - domain: alternate.domain
           - domain: my.domain
             hostSuffix: -dc1
       tls:
         mode: verify-ca
         clientCert:
           caCertSecret: client-ca-key-pair
           certManager:
             spec:
               issuerRef:
                 name: client-ca-issuer
                 kind: Issuer
                 group: cert-manager.io
         serverCert:
           caCertSecret: server-ca-key-pair
           certManager:
             spec:
               issuerRef:
                 name: server-ca-issuer
                 kind: Issuer
                 group: cert-manager.io
   EOF
   ```
   
After creating the cluster, add the other two required labels to the inheritedMetadata which are required metrics collection.

On Cluster B: Get the PGDGroup UID for the beacon-resource-id:

   ```
   export pgdgroup_uid="$(kubectl get pgdgroup external-pgd-group -n test-pgd -o jsonpath='{.metadata.uid}')"
   
   
   kubectl patch pgdgroup external-pgd-group -n test-pgd --type='merge' -p='
   spec:
     inheritedMetadata:
       labels:
         k8s.enterprisedb.io/beacon: enabled
         k8s.enterprisedb.io/beacon-project-id: "'"${project_pgdgroup}"'"
         k8s.enterprisedb.io/beacon-resource-id: "'"${pgdgroup_name}-${pgdgroup_uid}"'"
   '
   ```

### Label Descriptions:
   
   These labels are essential for:
   
   - k8s.enterprisedb.io/beacon: Enables beacon monitoring
   - k8s.enterprisedb.io/beacon-project-id: Associates metrics with the correct HCP project
   - k8s.enterprisedb.io/beacon-resource-id: Provides unique cluster identification using Kubernetes UID
   - All three labels will be inherited by pods/PVCs and used for metric labeling

### Verification in the HM console

Log in to the HM console and select the **Estate** tab.

The external PGDGroup should now appear with the collected metrics.

## Configuration

Core Configuration Options

| Parameter                                             | Description                              | Default            | Required              |
|-------------------------------------------------------|------------------------------------------|--------------------|-----------------------|
| agentConfig.accessKey                                 | HCP Machine User access key              | `""`               | Yes                   |
| agentConfig.projectID                                 | HCP Project ID                           | `""`               | Yes                   |
| agentConfig.beaconServer.host                         | HCP server hostname                      | `""`               | Yes                   |
| agentConfig.locationID                                | Agent location identifier                | `"standalone-k8s"` | Yes                   |
| namespace                                             | Kubernetes namespace                     | `"upm-beacon"`     | No                    |
| **image.repository**                                  | Beacon agent image                       | `""`               | Yes                   |
| **image.tag**                                         | Beacon agent version                     | `""`               | Yes                   |
| **otelImage.repository**                              | OTEL collector image                     | `""`               | Yes (if OTEL enabled) |
| **otelImage.tag**                                     | OTEL collector version                   | `""`               | Yes (if OTEL enabled) |
| **additionalCharts.cert-manager.install**             | Enable cert-manager subchart             | `false`            | No                    |
| **additionalCharts.opentelemetry-collector.install**  | Enable opentelemetry-collector subchart  | `false`            | No                    |
| **additionalCharts.kube-state-metrics.install**       | Enable kube-state-metrics subchart       | `false`            | No                    |
| **additionalCharts.prometheus-node-exporter.install** | Enable prometheus-node-exporter subchart | `false`            | No                    |
| **opentelemetry-collector.image.repository**          | OpenTelemetry collector image repository | `""`               | Yes (if OTEL enabled) |
| **opentelemetry-collector.image.tag**                 | OpenTelemetry collector image tag        | `""`               | Yes (if OTEL enabled) |

### Security context configuration for standard Kubernetes clusters

Configure your custom-values.yaml with default secure settings which include user and group IDs:

   ``` Yaml
   securityContext:
     allowPrivilegeEscalation: false
     capabilities:
       drop:
         - ALL
     privileged: false
     readOnlyRootFilesystem: true
     runAsNonRoot: true
     runAsUser: 10000
     runAsGroup: 10000
   
   podSecurityContext:
     seccompProfile:
       type: RuntimeDefault
   ```

### Security context configuration for OpenShift clusters

1. Enable the OpenShift mode to automatically configure security contexts for SCC (Security contenxt configuration) compatibility.

1. Enable OpenShift support in your custom-values.yaml file:

   ```yaml
   openshift:
     enabled: true
   ```

When the value is set to `openshift.enabled: true`, the chart automatically:

- Omits `runAsUser` and `runAsGroup` from the container security contexts, allowing SCC (Security contenxt configuration) to assign them.
- Stores other security settings like capabilities drop, readOnlyRootFilesystem, etc.
- Uses numeric UID 65532 for the OpenTelemetry collector that is required for distroless image validation.

1. Alternatively, specify your configuration via the command line using the -f custom-values.yaml flag:

   ```Yaml
   In your custom-values.yaml for OpenShift
   securityContext:
     allowPrivilegeEscalation: false
     capabilities:
       drop:
         - ALL
     privileged: false
     readOnlyRootFilesystem: true
     runAsNonRoot: true
     ```

1. Leave the fields below empty as OpenShift will assign them automatically:
     
    ```
    runAsUser:
    runAsGroup:
    ```
1. When deploying to OpenShift, ensure the fsGroup field remains undefined to allow the platform to manage volume permissions automatically:

   ```
   podSecurityContext:
     seccompProfile:
       type: RuntimeDefault
     # Leave fsGroup empty for OpenShift
     # fsGroup:
   ```
1. To ensure OpenShift compatibility, configure the OpenTelemetry Collector subchart with a restricted security context:

   ```
   opentelemetry-collector:
     podSecurityContext:
       seccompProfile:
         type: RuntimeDefault
     securityContext:
       runAsNonRoot: true
       allowPrivilegeEscalation: false
       capabilities:
         drop:
           - ALL
   ```

!!! note
- Distroless Image Support: The UID/GID `65532` is the standard "nonroot" user for distroless images, which lacks a traditional `/etc/passwd` file.

- Numeric Validation: Setting a specific numeric ID is required for the runAsNonRoot: true directive to verify the user identity correctly.

- OpenShift Compatibility: While OpenShift's Privileged Security Context Constraint (SCC) allows any UID, using 65532 ensures a consistent, secure identity across both OpenShift and standard Kubernetes environments.
!!!


**Key configuration notes:**

- OpenShift ID Management: The runAsUser, runAsGroup, and fsGroup fields are left empty to allow OpenShift’s Security Context Constraints (SCC) to automatically assign the correct UID and GID ranges for the project.

- Security Context Activation: Setting enabled: true ensures the subchart applies the specified security settings rather than falling back to its internal defaults.

- Non-Root Enforcement: The runAsNonRoot: true flag remains active to guarantee that the container never runs with root privileges, even when IDs are dynamically assigned by the platform.

- Seccomp Hardening: Utilizing the RuntimeDefault profile ensures the container uses the container runtime's default secure syscall filter.

  ```
     securityContext:
       enabled: true
       runAsNonRoot: true
       runAsUser:
       runAsGroup:
       fsGroup:
       seccompProfile:
         type: RuntimeDefault
  ```
         
1. Harden the container-level security context by enforcing a read-only filesystem, disabling privilege escalation, and dropping all default Linux capabilities:

   ```
     containerSecurityContext:
       readOnlyRootFilesystem: true
       allowPrivilegeEscalation: false
       capabilities:
         drop:
           - ALL
   ```
   
   Alternatively, you can apply these settings via the command line:
   
   ```
   helm upgrade --install beacon-agent-k8s \
     . \
     --namespace upm-beacon \
     --create-namespace \
     # ... your other flags ...
     --set securityContext.runAsUser=null \
     --set securityContext.runAsGroup=null \
     --set podSecurityContext.fsGroup=null \
     --set opentelemetry-collector.securityContext.runAsUser=null \
     --set opentelemetry-collector.securityContext.runAsGroup=null \
     --set kube-state-metrics.podSecurityContext=null
   ```

### Requirements for OpenShift:

OpenShift assigns a specific UID range to each namespace. 

For example: 1000880000-1000889999.

Specifying an explicit runAsUser like 10000 often falls outside this range, triggering Security Context Constraints (SCC) violations. By leaving these values empty, OpenShift's admission controller automatically assigns appropriate values from the allowed range.

### runAsUser differences between components:

| Component               | runAsUser | Reason                                                                                                                                                                                     |
|-------------------------|-----------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| beacon-agent            | Empty     | Uses restricted SCC (Security Context Constraints); OpenShift assigns UID from namespace range                                                                                             |
| kube-state-metrics      | Empty     | Uses restricted SCC (Security Context Constraints); OpenShift assigns UID from namespace range                                                                                             |
| opentelemetry-collector | 65532     | Uses privileged SCC (Security Context Constraints) for hostPath or hostPorts.                                                                                                              |
| opentelemetry-collector | 65532     | Requires explicit numeric UID because `runAsNonRoot: true` cannot verify non-numeric users like `nonroot`. Privileged SCC (Security Context Constraints) allows any UID, so `65532` works. |

### OpenShift: Granting SCC (Security Context Constraints) for Daemonsets 

To collect node-level metrics and logs on OpenShift, the opentelemetry-collector and prometheus-node-exporter subcharts require elevated permissions to access hostPath, hostPorts, and hostNetwork. Grant the privileged Security Context Constraint (SCC) to their respective service accounts using the commands below.

!!! note
Since OpenShift includes a built-in node-exporter, your deployment may conflict with existing monitoring; consider using the platform's native exporter instead.
!!!

### Openshift permission setup and deployment

1. Create the namespace (if not already present):

   ```
   oc new-project upm-beacon
   ```

1. Grant the Privileged SCC:

  ``` 
   oc adm policy add-scc-to-user privileged \
     system:serviceaccount:upm-beacon:beacon-agent-k8s-opentelemetry-collector
   
   oc adm policy add-scc-to-user privileged \
     system:serviceaccount:upm-beacon:beacon-agent-k8s-prometheus-node-exporter
  ```

1. Deploy or Update the Helm Chart:
   
   ```
   helm upgrade --install beacon-agent . -f custom-values.yaml -n upm-beacon
   ```

1. Restart the Daemonsets:
   ```
   kubectl -n upm-beacon rollout restart daemonset beacon-agent-k8s-opentelemetry-collector-agent
   kubectl -n upm-beacon rollout restart daemonset beacon-agent-k8s-prometheus-node-exporter
   ```

!!! note 
On OpenShift, prometheus-node-exporter may conflict with OpenShift's built-in node-exporter. 
For example if port 9100 is already in use. 
It's recommended to set additionalCharts.prometheus-node-exporter.install: false and rely on the opentelemetry-collector for container metrics via cadvisor/kubelet scraping.
!!!

---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP RULE
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/DROP_RULE.md
editTarget: originalFilePath

---

Removes a rewrite rule.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
DROP RULE [IF EXISTS] <name> ON <table_name> [CASCADE | RESTRICT]
```

<a id="section3"></a>

## Description

`DROP RULE` drops a rewrite rule from a table or view.

<a id="section4"></a>

## Parameters

IF EXISTS
Do not throw an error if the rule does not exist. WarehousePG issues a notice in this case.

name
The name of the rule to remove.

table_name
The name (optionally schema-qualified) of the table or view that the rule applies to.

CASCADE
Automatically drop objects that depend on the rule, and in turn all objects that depend on those objects.

RESTRICT
Refuse to drop the rule if any objects depend on it. This is the default.

<a id="section5"></a>

## Examples

Remove the rewrite rule `sales_2006` on the table `sales`:

```
DROP RULE sales_2006 ON sales;
```

<a id="section6"></a>

## Compatibility

`DROP RULE` is a WarehousePG extension, as is the entire query rewrite system.

<a id="section7"></a>

## See Also

[ALTER RULE](ALTER_RULE.mdx), [CREATE RULE](CREATE_RULE.mdx)

**Parent topic:** [SQL Commands](./)

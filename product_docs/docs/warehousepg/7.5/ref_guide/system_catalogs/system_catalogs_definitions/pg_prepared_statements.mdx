---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: pg_prepared_statements
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/system_catalogs/system_catalogs_definitions/pg_prepared_statements.md
editTarget: originalFilePath

---

The `pg_prepared_statements` view displays all of the prepared statements that are available in the current session. See [PREPARE](../../sql_commands/PREPARE.mdx) for more information about prepared statements.

`pg_prepared_statements` contains one row for each prepared statement. Rows are added to the view when a new prepared statement is created and removed when a prepared statement is released (for example, with the [DEALLOCATE](../../sql_commands/DEALLOCATE.mdx) command).

The `pg_prepared_statements` view is read-only.

| name              | type        | description                                                                                                                                                                                                                                                                                         |
| ----------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `name`            | text        | The identifier of the prepared statement.                                                                                                                                                                                                                                                           |
| `statement`       | text        | The query string submitted by the client to create this prepared statement. For prepared statements created via SQL, this is the `PREPARE` statement submitted by the client. For prepared statements created via the frontend/backend protocol, this is the text of the prepared statement itself. |
| `prepare_time`    | timestamptz | The time at which the prepared statement was created.                                                                                                                                                                                                                                               |
| `parameter_types` | regtype\[]  | The expected parameter types for the prepared statement in the form of an array of `regtype`. The object identifier corresponding to an element of this array can be obtained by casting the `regtype` value to `oid`.                                                                              |
| `from_sql`        | boolean     | `true` if the prepared statement was created via the `PREPARE` SQL command, `false` if the statement was prepared via the frontend/backend protocol.                                                                                                                                                |

**Parent topic:** [System Catalogs Definitions](./)

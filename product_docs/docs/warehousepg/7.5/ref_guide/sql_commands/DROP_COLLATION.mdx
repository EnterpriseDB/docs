---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP COLLATION
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/DROP_COLLATION.md
editTarget: originalFilePath

---

Removes a previously defined collation.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
DROP COLLATION [ IF EXISTS ] <name> [ CASCADE | RESTRICT ]
```

<a id="section3"></a>

## Description

`DROP COLLATION` removes a previously defined collation. To be able to drop a collation, you must own the collation.

<a id="section4"></a>

## Parameters

`IF EXISTS`
Do not throw an error if the collation does not exist. A notice is issued in this case.

name
The name of the collation. The collation name can be schema-qualified.

`CASCADE`
Automatically drop objects that depend on the collation, and in turn all objects that depend on those objects.

`RESTRICT`
Refuse to drop the collation if any objects depend on it. This is the default.

<a id="section6"></a>

## Examples

To drop the collation named `german`:

```
DROP COLLATION german;
```

<a id="section7"></a>

## Compatibility

The `DROP COLLATION` command conforms to the SQL standard, apart from the `IF EXISTS` option, which is a WarehousePG extension.

<a id="section8"></a>

## See Also

[ALTER COLLATION](ALTER_COLLATION.mdx), [CREATE COLLATION](CREATE_COLLATION.mdx)

**Parent topic:** [SQL Commands](./)

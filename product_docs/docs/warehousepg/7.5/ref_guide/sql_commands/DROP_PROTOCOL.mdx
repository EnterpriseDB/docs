---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP PROTOCOL
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/DROP_PROTOCOL.md
editTarget: originalFilePath

---

Removes a data access protocol from a database.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
DROP PROTOCOL [IF EXISTS] <name>
```

<a id="section3"></a>

## Description

`DROP PROTOCOL` removes the specified protocol from a database. You specify a protocol name in the `CREATE EXTERNAL TABLE` command to read data from or write data to an external data source.

You must be a superuser or the protocol owner to drop a protocol.

> **Caution** If you drop a data access prococol, external tables that have been defined specifying the protocol will no longer be able to access the external data source.

<a id="section4"></a>

## Parameters

IF EXISTS
Do not throw an error if the protocol does not exist. WarehousePG issues a notice in this case.

name
The name of an existing data access protocol.

<a id="section5"></a>

## Notes

Dropping a data access protocol, does not drop the protocol's call handlers. You must drop these functions manually.

Be sure to remove any shared libraries that were used by the protocol from the WarehousePG hosts.

<a id="section6"></a>

## Compatibility

`DROP PROTOCOL` is a WarehousePG extension.

<a id="section7"></a>

## See Also

[CREATE EXTERNAL TABLE](CREATE_EXTERNAL_TABLE.mdx), [CREATE PROTOCOL](CREATE_PROTOCOL.mdx)

**Parent topic:** [SQL Commands](./)

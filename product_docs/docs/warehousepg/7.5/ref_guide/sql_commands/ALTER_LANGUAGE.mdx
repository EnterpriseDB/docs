---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: ALTER LANGUAGE
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/ALTER_LANGUAGE.md
editTarget: originalFilePath

---

Changes the definition of a procedural language.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
ALTER [ PROCEDURAL ] LANGUAGE <name> RENAME TO <new_name>
ALTER [ PROCEDURAL ] LANGUAGE <name> OWNER TO { <new_owner> | CURRENT_USER | SESSION_USER }
```

<a id="section3"></a>

## Description

`ALTER LANGUAGE` changes the definition of a procedural language for a specific database. Definition changes supported include renaming the language or assigning a new owner. You must be superuser or the owner of the language to use `ALTER LANGUAGE`.

<a id="section4"></a>

## Parameters

name
Name of a language.

new_name
The new name of the language.

new_owner
The new owner of the language.

<a id="section5"></a>

## Compatibility

There is no `ALTER LANGUAGE` statement in the SQL standard.

<a id="section6"></a>

## See Also

[CREATE LANGUAGE](CREATE_LANGUAGE.mdx), [DROP LANGUAGE](DROP_LANGUAGE.mdx)

**Parent topic:** [SQL Commands](./)

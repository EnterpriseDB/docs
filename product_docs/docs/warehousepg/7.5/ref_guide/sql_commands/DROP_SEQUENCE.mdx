---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP SEQUENCE
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/DROP_SEQUENCE.md
editTarget: originalFilePath

---

Removes a sequence.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
DROP SEQUENCE [IF EXISTS] <name> [, ...] [CASCADE | RESTRICT]
```

<a id="section3"></a>

## Description

`DROP SEQUENCE` removes sequence number generators. You must own the sequence to drop it (or be a superuser).

<a id="section4"></a>

## Parameters

IF EXISTS
Do not throw an error if the sequence does not exist. WarehousePG issues a notice in this case.

name
The name (optionally schema-qualified) of the sequence to remove.

CASCADE
Automatically drop objects that depend on the sequence, and in turn all objects that depend on those objects.

RESTRICT
Refuse to drop the sequence if any objects depend on it. This is the default.

<a id="section5"></a>

## Examples

Remove the sequence `myserial`:

```
DROP SEQUENCE myserial;
```

<a id="section6"></a>

## Compatibility

`DROP SEQUENCE` conforms to the SQL standard, except that the standard allows only one sequence to be dropped per command. Also, the `IF EXISTS` option is a WarehousePG extension.

<a id="section7"></a>

## See Also

[ALTER SEQUENCE](ALTER_SEQUENCE.mdx), [CREATE SEQUENCE](CREATE_SEQUENCE.mdx)

**Parent topic:** [SQL Commands](./)

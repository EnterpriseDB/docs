---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DEALLOCATE
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/7x/ref_guide/sql_commands/DEALLOCATE.md
editTarget: originalFilePath

---

Deallocates a prepared statement.

<a id="section2"></a>

## Synopsis

<div id="sql_command_synopsis"></div>

```
DEALLOCATE [PREPARE] { <name> | ALL }
```

<a id="section3"></a>

## Description

`DEALLOCATE` is used to deallocate a previously prepared SQL statement. If you do not explicitly deallocate a prepared statement, it is deallocated when the session ends.

For more information on prepared statements, see [PREPARE](PREPARE.mdx).

<a id="section4"></a>

## Parameters

PREPARE
Optional key word which is ignored.

name
The name of the prepared statement to deallocate.

ALL
Deallocate all prepared statements

<a id="section5"></a>

## Examples

Deallocate the previously prepared statement named `insert_names`:

```
DEALLOCATE insert_names;
```

<a id="section6"></a>

## Compatibility

The SQL standard includes a `DEALLOCATE` statement, but it is only for use in embedded SQL.

<a id="section7"></a>

## See Also

[EXECUTE](EXECUTE.mdx), [PREPARE](PREPARE.mdx)

**Parent topic:** [SQL Commands](./)

---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: Enabling Compression
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/6x/admin_guide/managing/compression.md
editTarget: originalFilePath

---

You can configure WarehousePG to use data compression with some database features and with some utilities.Compression reduces disk usage and improves I/O across the system, however, it adds some performance overhead when compressing and decompressing data.

You can configure support for data compression with these features and utilities. See the specific feature or utility for information about support for compression.

-   Append-optimized tables support compressing table data. See [CREATE TABLE](../../ref_guide/sql_commands/CREATE_TABLE.mdx).
-   User-defined data types can be defined to compress data. See [CREATE TYPE](../../ref_guide/sql_commands/CREATE_TYPE.mdx).
-   The external table protocols [gpfdist](../external/external-tables/gpfdist-protocol.mdx) ([gpfdists](../external/external-tables/gpfdists-protocol.mdx)), [s3](../external/external-tables/s3-protocol.mdx), and [pxf](../external/pxf-overview.mdx) support compression when accessing external data. For information about external tables, see [CREATE EXTERNAL TABLE](../../ref_guide/sql_commands/CREATE_EXTERNAL_TABLE.mdx).
-   Workfiles (temporary spill files that are created when running a query that requires more memory than it is allocated) can be compressed. See the server configuration parameter [gp_workfile_compression](../../ref_guide/config_params/guc-list.mdx).
-   The WarehousePG utilities gpbackup, gprestore, [gpload](../../ref_guide/utility_guide/reference/gpload.mdx), and [gplogfilter](../../ref_guide/utility_guide/reference/gplogfilter.mdx) support compression.

For some compression algorithms (such as zlib) WarehousePG requires software packages installed on the host system. For information about required software packages, see the *WarehousePG Installation Guide*.

**Parent topic:** [Managing a WarehousePG cluster](./)

---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: gp_pitr
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/6x/ref_guide/modules/gp_pitr.md
editTarget: originalFilePath

---

The `gp_pitr` module supports implementing Point-in-Time Recovery for WarehousePG 6. In service of this it creates a new view -- `gp_stat_archiver` -- as well as two user-defined functions that are called internally.

The `gp_pitr` module is a WarehousePG extension.

<a id="topic_reg"></a>

## Installing and Registering the Module

The `gp_pitr` module is installed when you install WarehousePG. Before you can use the view defined in the module, you must register the `gp_pitr` extension in each database where you want to use the function, using the following command:

```
CREATE EXTENSION gp_pitr;
```

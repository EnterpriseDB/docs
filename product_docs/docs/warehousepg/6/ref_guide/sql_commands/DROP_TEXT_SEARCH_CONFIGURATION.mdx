---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP TEXT SEARCH CONFIGURATION
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/6x/ref_guide/sql_commands/DROP_TEXT_SEARCH_CONFIGURATION.md
editTarget: originalFilePath

---

<a id="topic1"></a><a id="az137232"></a><a id="sql_command_desc"></a>

Removes a text search configuration.

<a id="section2">

</a>

## Synopsis

<a id="sql_command_synopsis"></a>

```
DROP TEXT SEARCH CONFIGURATION [ IF EXISTS ] <name> [ CASCADE | RESTRICT ]
```

<a id="section3">

</a>

## Description

`DROP TEXT SEARCH CONFIGURATION` drops an existing text search configuration. To run this command you must be the owner of the configuration.

<a id="section4">

</a>

## Parameters

-   `IF EXISTS`

    Do not throw an error if the text search configuration does not exist. A notice is issued in this case.

-   `name`

    The name (optionally schema-qualified) of an existing text search configuration.

-   `CASCADE`

    Automatically drop objects that depend on the text search configuration.

-   `RESTRICT`

    Refuse to drop the text search configuration if any objects depend on it. This is the default.

<a id="section5">

</a>

## Examples

Remove the text search configuration my_english:

```
DROP TEXT SEARCH CONFIGURATION my_english;
```

This command will not succeed if there are any existing indexes that reference the configuration in `to_tsvector` calls. Add `CASCADE` to drop such indexes along with the text search configuration.

<a id="section6">

</a>

## Compatibility

There is no `DROP TEXT SEARCH CONFIGURATION` statement in the SQL standard.

<a id="section7">

</a>

## See Also

[ALTER TEXT SEARCH CONFIGURATION](ALTER_TEXT_SEARCH_CONFIGURATION.mdx), [CREATE TEXT SEARCH CONFIGURATION](CREATE_TEXT_SEARCH_CONFIGURATION.mdx)

**Parent topic:** [SQL Commands](./)

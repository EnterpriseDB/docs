---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: DROP DOMAIN
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/6x/ref_guide/sql_commands/DROP_DOMAIN.md
editTarget: originalFilePath

---

<a id="topic1"></a><a id="cs20941"></a><a id="sql_command_desc"></a>

Removes a domain.

<a id="section2">

</a>

## Synopsis

<a id="sql_command_synopsis"></a>

```
DROP DOMAIN [IF EXISTS] <name> [, ...]  [CASCADE | RESTRICT]
```

<a id="section3">

</a>

## Description

`DROP DOMAIN` removes a previously defined domain. You must be the owner of a domain to drop it.

<a id="section4">

</a>

## Parameters

-   IF EXISTS

    Do not throw an error if the domain does not exist. A notice is issued in this case.

-   name

    The name (optionally schema-qualified) of an existing domain.

-   CASCADE

    Automatically drop objects that depend on the domain (such as table columns).

-   RESTRICT

    Refuse to drop the domain if any objects depend on it. This is the default.

<a id="section5">

</a>

## Examples

Drop the domain named `zipcode`:

```
DROP DOMAIN zipcode;
```

<a id="section6">

</a>

## Compatibility

This command conforms to the SQL standard, except for the `IF EXISTS` option, which is a WarehousePG extension.

<a id="section7">

</a>

## See Also

[ALTER DOMAIN](ALTER_DOMAIN.mdx), [CREATE DOMAIN](CREATE_DOMAIN.mdx)

**Parent topic:** [SQL Commands](./)

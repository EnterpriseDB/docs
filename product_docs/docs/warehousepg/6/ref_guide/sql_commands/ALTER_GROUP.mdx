---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: ALTER GROUP
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/docs/6x/ref_guide/sql_commands/ALTER_GROUP.md
editTarget: originalFilePath

---

<a id="topic1"></a><a id="ao20941"></a><a id="sql_command_desc"></a>

Changes a role name or membership.

<a id="section2">

</a>

## Synopsis

<a id="sql_command_synopsis"></a>

```
ALTER GROUP <groupname> ADD USER <username> [, ... ]

ALTER GROUP <groupname> DROP USER <username> [, ... ]

ALTER GROUP <groupname> RENAME TO <newname>
```

<a id="section3">

</a>

## Description

`ALTER GROUP` changes the attributes of a user group. This is an obsolete command, though still accepted for backwards compatibility, because users and groups are superseded by the more general concept of roles. See [ALTER ROLE](ALTER_ROLE.mdx) for more information.

The first two variants add users to a group or remove them from a group. Any role can play the part of groupname or username. The preferred method for accomplishing these tasks is to use [GRANT](GRANT.mdx) and [REVOKE](REVOKE.mdx).

<a id="section4">

</a>

## Parameters

-   groupname

    The name of the group (role) to modify.

-   username

    Users (roles) that are to be added to or removed from the group. The users (roles) must already exist.

-   newname

    The new name of the group (role).

<a id="section5">

</a>

## Examples

To add users to a group:

```
ALTER GROUP staff ADD USER karl, john;
```

To remove a user from a group:

```
ALTER GROUP workers DROP USER beth;
```

<a id="section6">

</a>

## Compatibility

There is no `ALTER GROUP` statement in the SQL standard.

<a id="section7">

</a>

## See Also

[ALTER ROLE](ALTER_ROLE.mdx), [GRANT](GRANT.mdx), [REVOKE](REVOKE.mdx)

**Parent topic:** [SQL Commands](./)

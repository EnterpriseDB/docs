---
title: "Numeric Bitwise Functions"
---

<div id="numeric_functions" class="registered_link"></div>

The following section describes numeric bitwise functions that accept numeric input and returns numeric bitwise values.

## BITAND

The `BITAND` function performs a bitwise `AND` operation and returns a numeric value based on the data type of input argument. The signature is:

```text
BITAND(<expr1>, <expr2>)
```

The input data type of `expr1` and `expr2` is a `NUMERIC` value on which the bitwise `AND` operation will be performed. If either argument to `BITAND` is `NULL`, the result is a `NULL` value.

The `BITAND` compares each bit of `expr1` value to the corresponding bit of `exp2` using a bitwise `AND` operation and converts the value of an input argument to `NUMERIC`.

**Return Types**

The input data type of `expression` determines the return type. The following table illustrates the return type for each input type.

| Input Type  | Return Type |
| ----------- | ----------- |
| `NUMERIC`   | `NUMERIC`   |
| `BIGINT`    | `BIGINT`    |
| `INTEGER`   | `INTEGER`   |
| `SMALLINT`  | `SMALLINT`  |
| `INET`      | `INET`      |
| `MACADDR`   | `MACADDR`   |
| `MACADDR8`  | `MACADDR8`  |

**Examples**

The following example performs an `AND` operation on the numbers 10 and 11.

```text
edb=# SELECT BITAND(10,11) FROM DUAL;
 bitand 
--------
     10
(1 row)
```

# BITOR

The `BITOR` function performs a bitwise `OR` operation and returns a numeric value based on the data type of input argument. The signature is:

```text
BITOR(<expr1>, <expr2>)
```

The `BITOR` compares each bit of `expr1` value to the corresponding bit of `exp2` using a bitwise `OR` operation and converts the value of an input argument to `NUMERIC`.

**Return Types**

The input data type of `expression` determines the return type. The following table illustrates the return type for each input type.

| Input Type  | Return Type |
| ----------- | ----------- |
| `NUMERIC`   | `NUMERIC`   |
| `BIGINT`    | `BIGINT`    |
| `INTEGER`   | `INTEGER`   |
| `SMALLINT`  | `SMALLINT`  |
| `INET`      | `INET`      |
| `MACADDR`   | `MACADDR`   |
| `MACADDR8`  | `MACADDR8`  |

**Examples**

The following example performs an `OR` operation on the numbers 10 and 11.

```text
edb=# SELECT BITOR(10,11) FROM DUAL;
 bitor 
-------
    11
(1 row)
```

# BITWISE OPERATORS

The bitwise operators perform the same operation as `BITAND` and `BITOR`. The bitwise operators are:

-  `&`: `(Bitwise AND)` Return the result of a bitwise `AND` operation performed on two numeric values.

-  `|`: `(Bitwise OR)` Return the result of a bitwise `OR` operation performed on two numeric values.

**Examples**

The following example uses `&` operator and returns the output as `BITAND` of the input.

```text
edb=# SELECT 10&11 FROM DUAL;
 ?column? 
----------
       10
(1 row)
```

The following example uses the `|` operator and returns the output as `BITOR` of the input.

```text
edb=# SELECT 10|11 FROM DUAL;
 ?column? 
----------
       11
(1 row)
```


---
title: "QUALIFIED_SQL_NAME"
---

The `QUALIFIED_SQL_NAME` procedure checks to determine if the input string is a qualified SQL name. A quotation mark is allows within the input string.


```sql
QUALIFIED_SQL_NAME(
  <str> VARCHAR2 CHARACTER SET ANY_CS)
RETURN VARCHAR2 CHARACTER SET str%CHARSET;
```

## Parameters

`str`

 The input string.


---
title: "ENQUOTE_NAME"
---

The `ENQUOTE_NAME` procedure procedure wraps quotation marks around a string literal unless it is already enclosed in quotation marks.


```sql
ENQUOTE_NAME(
  <str> VARCHAR2,
  <capitalize> BOOLEAN DEFAULT TRUE)
RETURN VARCHAR2;
```

## Parameters

`str`

 The string to be wrapped in single quotes.



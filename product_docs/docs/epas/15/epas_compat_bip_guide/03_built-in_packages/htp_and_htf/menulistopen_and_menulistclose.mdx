---
title: template
---

This generates the `<MENU>` and `</MENU>` tags, which create a list that presents one line for each item. 

## Syntax

The following is the syntax for HTP:
```
htp.menulistOpen;

htp.menulistClose;
```

The following is the syntax for HTF:
```
htf.menulistOpen return varchar2;

htf.menulistClose return varchar2;
```

---
title: "COLUMN_VALUE_ROWID"
---

The `COLUMN_VALUE_ROWID` procedure defines a column that you can select from a specified cursor. 

???THIS IS ALL GUESSWORK BECAUSE I'M NOT SEEING A CORRESPONDING ORACLE PROCEDURE???

```sql
DEFINE_COLUMN_RAW(<c> NUMBER, <position> NUMBER, <column> NUMBER)
```

## Parameters

`c`

 Cursor ID of the cursor from which a row is selected.

`position`

 Relative position of the column in the row being defined. The first column in a statement has position 1.

`column`

 Value of the column. 



### Examples


```sql



```

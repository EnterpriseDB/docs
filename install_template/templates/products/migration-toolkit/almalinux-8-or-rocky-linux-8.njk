{% extends "products/migration-toolkit/base.njk" %}
{% set platformBaseTemplate = "almalinux-8-or-rocky-linux-8" %}
{% set packageManager = "dnf" %}
{% block prerequisites %}
- Address other prerequisites
  ```shell
  #  Install the EPEL repository:
  sudo {{ packageManager }} -y install https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm

  {% block redhatConfig %}#  Enable additional repositories to resolve dependencies:
  ARCH=$( /bin/arch ) subscription-manager repos --enable "codeready-builder-for-rhel-8-${ARCH}-rpms"{% endblock redhatConfig %}
  ```
{% endblock prerequisites %}
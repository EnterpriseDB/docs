import React from 'react';
import ChevronDownSvg from '../../../static/edb-icons/chevron-down.svg';

function ChevronDown({ className, width, height }) {
  return <ChevronDownSvg className={className} height={height} width={width} />;
}

export default ChevronDown;

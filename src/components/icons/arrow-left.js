import React from 'react';
import ArrowLeftSvg from '../../../static/edb-icons/arrow-left.svg';

function ArrowLeft({ className, width, height }) {
  return <ArrowLeftSvg className={className} height={height} width={width} />;
}

export default ArrowLeft;

import React from 'react';
import ChevronRightSvg from '../../../static/icons/chevron-right.svg';

function ChevronRight({ className, width, height }) {
  return <ChevronRightSvg className={className} height={height} width={width} />;
}

export default ChevronRight;

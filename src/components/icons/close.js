import React from 'react';
import CloseSvg from '../../../static/edb-icons/close.svg';

function Close({ className, width, height }) {
  return <CloseSvg className={className} height={height} width={width} />;
}

export default Close;

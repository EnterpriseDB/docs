---
title: "pgvector"
categories: 
 - /edb-postgres-ai/extensions-and-tools
 - /pg_extensions/
directoryDefaults: 
  product : pgvector
indexCards: none
navigation:
   - rel_notes
   - compatibility
   - installing
   - configure
   - using
   - integration
   - security
   - operations
---

`pgvector` is an open-source extension for PostgreSQL that allows you to store, query, and index vector embeddings directly within your database.

`pgvector` is supported on the same platforms as the Postgres distribution you're using. For details, see the [Compatibility](compatibility) page.

---
title: pgfs schema objects
navTitle: Reference
---

When you crate the PGFS extension, it automatically initializes a dedicated environment within your database to manage external files.

### pgfs schema

To keep your database organized and avoid naming conflicts with your existing data, all internal PGFS objects such as tables, views, functions are isolated within a dedicated `pgfs` schema.

1. Functions

   These functions are created in the `pgfs` schema to create, manage and view the external storage locations:

   | Function Name                       | Return Type | Description                                                                                            |
   |-------------------------------------|-------------|--------------------------------------------------------------------------------------------------------|
   | `pgfs.create_storage_location`      | TEXT        | Creates a foreign server and user mapping for cloud storage using a URL, options, and credentials.     |
   | `pgfs.list_storage_locations`       | TABLE       | Returns a list of all PGFS servers including their URLs, IDs, options, and credentials.                |
   | `pgfs.get_storage_location`         | TABLE       | Retrieves details for a specific storage location by name.                                             |
   | `pgfs.update_storage_location`      | TEXT        | Replaces an existing storage location with new configuration details.                                  |
   | `pgfs.delete_storage_location`      | VOID        | Removes the foreign server and user mapping for a storage location.                                    |
   | `pgfs.create_foreign_table`         | TEXT        | Creates a foreign table with a standard schema (key, size, last_modified, body) on a specified server. |
   | `pgfs.set_default_storage_location` | TEXT        | Updates the pgfs.config table to set a global default storage location.                                |

2. Catalog tables

   The extension creates internal catalog tables that can help track your storage locations. These tables store:

   - **Endpoint URLS**: Where the storage is located. For example, AWS S3, Google Cloud Storage (GCP), or Azure Blob.

   - **Authentication config**: References to the keys of IAM roles needed to access the data.

   - **Path mapping**: Definitions of which virtual paths in Postgres map to which physical paths in the cloud.

3. Integration points for Pipelines

   `PGFS` is a dependency for AI Accelerator Pipelines. It creates the hooks necessary for:

   - **Volumes**: Virtual storage abstractions that Pipelines use as data sources.

   - **Format handlers**: The logic required to identify and process different data formats like `Text` or `Image` stored in the cloud.


For more information on using these components, see [Using PGFS](./using/).

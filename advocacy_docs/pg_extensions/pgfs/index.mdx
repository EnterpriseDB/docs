---
title: PGFS
indexCards: simple
navigation:
   - installing
   - configuring
   - settings
   - managing
   - using
categories:
 - /pg_extensions/
directoryDefaults:
  product: pgfs
deepToc: true
---

PGFS (Postgres File System) is a key component of EDB Postgres AI Pipelines, implemented as a Foreign Data Wrapper (FDW) and installed as an extension. It allows a Postgres database to access, store, and retrieve files from external storage providers. To use a `PGFS` storage location with AIDB, a volume must be created in AIDB.

**Purpose**: It abstracts the complexity of external storage, enabling AI Pipelines to work with large or unstructured data like PDFs, images, or audio that isn't suitable for standard relational tables.

**Storage Support**: Supports S3-compatible buckets (AWS, MinIO, etc.), Google Cloud Storage, and local file systems.
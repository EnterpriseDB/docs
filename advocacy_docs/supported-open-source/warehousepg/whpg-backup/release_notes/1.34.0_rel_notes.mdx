---
# Imported from the WHPG docs repo (https://github.com/warehouse-pg/whpg-docs)
# Changes made here may be overwritten. Prefer making changes in the source repo instead.
# See the originalFilePath value below for a link to the source of this file in the WHPG docs repo.
#
# To make changes that apply _only_ to EDB's documentation, try to cleanly separate those changes in the content
# (e.g. separate lines or ideally paragraphs or even sections, with clear indications that the content is EDB-specific) and 
# reach out to the docs team to discuss.

title: WarehousePG Backup and Restore 1.34.0 release notes
navTitle: Version 1.34.0
originalFilePath: >-
  https://github.com/warehouse-pg/whpg-docs/blob/main/docs/whpg-backup/release_notes/1.34.0_rel_notes.md
description: Release notes for version 1.34.0 of WarehousePG Backup and Restore.
editTarget: originalFilePath

---

**Release date:** August 25, 2026

WarehousePG Backup and Restore 1.34.0 includes new features, an improvement, a bug fix, and security updates.

## Features

-   **Manage backup history from the command line:** `gpbackup` adds five subcommands for inspecting and maintaining the [backup history database](../using.mdx#understanding-the-backup-history-database): `list-backups`, `find-table`, `display-report`, `delete-backup`, and `delete-backups-before`. See [Managing backup history](../using.mdx#managing-backup-history) for examples, or the [gpbackup reference](../reference/gpbackup.mdx#subcommands) for the full syntax.
-   **Skip extensions on restore:** `gprestore --exclude-extension` skips restoring specific extensions, useful when the target cluster doesn't have the extension's control file installed. See the [gprestore options](../reference/gprestore.mdx#options).

## Improvements

-   The backup history database now records `single_backup_dir`, `command_line`, and `object_count` for each backup. Opening an existing history database automatically adds the missing columns.

## Bug fixes

-   The [S3 storage plugin](../s3-plugin.mdx) now scopes `delete_directory` operations to the configured S3 folder and rejects unsafe delete targets, such as `..` traversal.

## Security

-   Updated dependencies to address known CVEs, including `golang.org/x/net`.

---
title: Configuring Grafana
navTitle: Configure Grafana
description: Learn how to configure a Grafana installation to display metrics about your WarehousePG cluster.
---

Configure Grafana to point to the data sources required for fetching metrics, and load the pre-configured dashboards to visualize the collected data.

## Add data sources

**Prometheus**
1. Log in to Grafana with admin privileges.
1. Go to **Connections** > **Data Sources** > **Add new data source**.
1. Select **Prometheus**.
1. Enter the Prometheus server URL (e.g., `http://<PROMETHEUS_HOST>:9090`).
1. Select **Save & Test**.

**Loki**
1. Log in to Grafana with admin privileges.
1. Go to **Connections** > **Data Sources** > **Add new data source**.
1. Select **Loki**.
1. Enter the Loki server URL (e.g., `http://<LOKI_HOST>:3100`).
1. Select **Save & Test**.


**Infinity data source for WHPG Exporter**
1. Log in to Grafana with admin privileges.
1. Navigate to **Connections** > **Add new connection**, search for **Infinity**, and select **Install**.
1. Go to **Connections** > **Data Sources** > **Add new data source**.
1. Select **Infinity**.
1. Enter the Exporter details under **Base URL** (e.g., `http://</WHPG_EXPORTER_HOST:<9187>/api/v1/query`).
1. Select **Save & Test**.


## Import the dashboards

EDB provides a set of JSON configuration files for Grafana. We understand that users have different needs for monitoring. The provided Grafana configurations provide a starting point for customization.

1. Download [WarehousePG dashboard JSON files](https://github.com/warehouse-pg/warehouse-pg-grafana-dashboards/tree/be231d0260a48d04281ffd28a583cd6b47dc23ba/Grafana/provisioning/dashboards).
    - **WHPG Cluster Dashboard**
    - **WHPG Cluster Details**
    - **WHPG Log Details**
    - **WHPG Query Details**
    - **WHPG Recommendations**
    - **WHPG Resource Details**
    - **WHPG Segment Details**

1. In Grafana, go to **Dashboards**.
1. Select the **New** dropdown menu and select **Import**.
1. Upload the JSON file or paste its content.
1. Select **Import**.
1. Repeat the same steps for all the provided dashboards.

## Verify the installation

You should now be able to view the metrics obtained from your WHPG cluster by using the Grafana dashboards.

